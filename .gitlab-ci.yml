build:
  stage: build
  script:
    - mvn compile

test:
  stage: test
  script:
    - mvn test

deploy:
  stage: deploy
  script:
    - mvn package
    - export PORT=5000
    - fuser -k 5000
    - nohup sh target/bin/simplewebapp > output.txt & fg
